<UserControl x:Class="NtfsAudit.App.Views.ResultsPanel"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">
  <Border Style="{StaticResource PanelBorderStyle}">
          <Grid>
            <Grid.RowDefinitions>
              <RowDefinition Height="Auto" />
              <RowDefinition Height="Auto" />
              <RowDefinition Height="*" />
            </Grid.RowDefinitions>
            <StackPanel Grid.Row="0">
              <TextBlock Text="{DynamicResource Results.Title}" Style="{StaticResource SectionHeaderText}" />
              <TextBlock Text="{DynamicResource Results.Empty}"
                         Style="{StaticResource MicrocopyText}"
                         TextWrapping="Wrap"
                         Visibility="{Binding HasNoScanResult, Converter={StaticResource BooleanToVisibilityConverter}}" />
              <WrapPanel Margin="0,0,0,4" IsEnabled="{Binding HasScanResult}" Visibility="{Binding HasScanResult, Converter={StaticResource BooleanToVisibilityConverter}}">
                <Border Style="{StaticResource InfoCardBorderStyle}" Background="#FFE3F2FD" BorderBrush="#FFBBDEFB">
                  <StackPanel>
                    <TextBlock Text="{DynamicResource Results.TotalAce}" Style="{StaticResource InfoLabelText}" />
                    <TextBlock Text="{Binding SummaryTotalEntries}" Style="{StaticResource InfoValueText}" />
                  </StackPanel>
                </Border>
                <Border Style="{StaticResource InfoCardBorderStyle}" Background="#FFFFEBEE" BorderBrush="#FFFFCDD2">
                  <StackPanel>
                    <TextBlock Text="{DynamicResource Results.Deny}" Style="{StaticResource InfoLabelText}" />
                    <TextBlock Text="{Binding SummaryDenyCount}" Style="{StaticResource InfoValueText}" />
                  </StackPanel>
                </Border>
                <Border Style="{StaticResource InfoCardBorderStyle}" Background="#FFF3E5F5" BorderBrush="#FFE1BEE7">
                  <StackPanel>
                    <TextBlock Text="{DynamicResource Results.HighRisk}" Style="{StaticResource InfoLabelText}" />
                    <TextBlock Text="{Binding SummaryHighRisk}" Style="{StaticResource InfoValueText}" />
                  </StackPanel>
                </Border>
                <Border Style="{StaticResource InfoCardBorderStyle}" Background="#FFE8F5E9" BorderBrush="#FFC8E6C9">
                  <StackPanel>
                    <TextBlock Text="{DynamicResource Results.File}" Style="{StaticResource InfoLabelText}" />
                    <TextBlock Text="{Binding SummaryFilesCount}" Style="{StaticResource InfoValueText}" />
                  </StackPanel>
                </Border>
                <Border Style="{StaticResource InfoCardBorderStyle}" Background="#FFFFF3E0" BorderBrush="#FFFFE0B2">
                  <StackPanel>
                    <TextBlock Text="{DynamicResource Results.Everyone}" Style="{StaticResource InfoLabelText}" />
                    <TextBlock Text="{Binding SummaryEveryoneCount}" Style="{StaticResource InfoValueText}" />
                  </StackPanel>
                </Border>
                <Border Style="{StaticResource InfoCardBorderStyle}" Background="#FFF5F5F5" BorderBrush="#FFE0E0E0">
                  <StackPanel>
                    <TextBlock Text="{DynamicResource Results.AuthenticatedUsers}" Style="{StaticResource InfoLabelText}" />
                    <TextBlock Text="{Binding SummaryAuthUsersCount}" Style="{StaticResource InfoValueText}" />
                  </StackPanel>
                </Border>
              </WrapPanel>
            </StackPanel>
            <StackPanel Grid.Row="1" Margin="0,8,0,8" Visibility="{Binding HasSelectedFolder, Converter={StaticResource BooleanToVisibilityConverter}}">
              <TextBlock Text="{DynamicResource Results.RightsLegend}" Style="{StaticResource SubsectionHeaderText}" />
              <WrapPanel Margin="0,0,0,6">
                <Border Background="#FF2F80ED" ToolTip="Permesso di lettura" Style="{StaticResource CompactBadgeBorderStyle}">
                  <TextBlock Text="R Read" Style="{StaticResource CompactBadgeText}" />
                </Border>
                <Border Background="#FF2F80ED" ToolTip="Visualizza elenco contenuti" Style="{StaticResource CompactBadgeBorderStyle}">
                  <TextBlock Text="L List" Style="{StaticResource CompactBadgeText}" />
                </Border>
                <Border Background="#FF2F80ED" ToolTip="Lettura ed esecuzione" Style="{StaticResource CompactBadgeBorderStyle}">
                  <TextBlock Text="RX R&amp;E" Style="{StaticResource CompactBadgeText}" />
                </Border>
                <Border Background="#FF27AE60" ToolTip="Scrittura" Style="{StaticResource CompactBadgeBorderStyle}">
                  <TextBlock Text="W Write" Style="{StaticResource CompactBadgeText}" />
                </Border>
                <Border Background="#FFF2994A" ToolTip="Modifica" Style="{StaticResource CompactBadgeBorderStyle}">
                  <TextBlock Text="M Modify" Style="{StaticResource CompactBadgeText}" />
                </Border>
                <Border Background="#FF9B51E0" ToolTip="Controllo completo" Style="{StaticResource CompactBadgeBorderStyle}">
                  <TextBlock Text="F Full" Style="{StaticResource CompactBadgeText}" />
                </Border>
                <Border Background="#FFEB5757" ToolTip="Accesso negato (Deny)" Style="{StaticResource CompactBadgeBorderStyle}">
                  <TextBlock Text="D Deny" Style="{StaticResource CompactBadgeText}" />
                </Border>
              </WrapPanel>
              <TextBlock Text="{DynamicResource Results.ProcessedFolder}" FontSize="11" Foreground="#FF546E7A" FontWeight="SemiBold" />
              <TextBlock Text="{Binding SelectedFolderName}" FontWeight="SemiBold" FontSize="13" Foreground="#FF0D47A1" TextTrimming="CharacterEllipsis" />
              <TextBlock Text="{Binding SelectedFolderPath}" FontSize="11" Foreground="#FF546E7A" TextTrimming="CharacterEllipsis" Margin="0,0,0,6" />
              <WrapPanel Margin="0,0,0,8">
                <Border Style="{StaticResource InfoCardBorderStyle}" Background="#FFF5F5F5" BorderBrush="#FFE0E0E0">
                  <StackPanel>
                    <TextBlock Text="{DynamicResource Results.PathKind}" Style="{StaticResource InfoLabelText}" />
                    <TextBlock Text="{Binding SelectedPathKind}" Style="{StaticResource InfoValueText}" />
                  </StackPanel>
                </Border>
                <Border Style="{StaticResource InfoCardBorderStyle}" Background="#FFF5F5F5" BorderBrush="#FFE0E0E0">
                  <StackPanel>
                    <TextBlock Text="{DynamicResource Results.PermissionLayers}" Style="{StaticResource InfoLabelText}" />
                    <TextBlock Text="{Binding SelectedPermissionLayers}" Style="{StaticResource InfoValueText}" />
                  </StackPanel>
                </Border>
                <Border Style="{StaticResource InfoCardBorderStyle}" Background="#FFF5F5F5" BorderBrush="#FFE0E0E0">
                  <StackPanel>
                    <TextBlock Text="{DynamicResource Results.Risk}" Style="{StaticResource InfoLabelText}" />
                    <TextBlock Text="{Binding SelectedRiskSummary}" Style="{StaticResource InfoValueText}" />
                  </StackPanel>
                </Border>
                <Border Style="{StaticResource InfoCardBorderStyle}" Background="#FFF5F5F5" BorderBrush="#FFE0E0E0">
                  <StackPanel>
                    <TextBlock Text="{DynamicResource Results.Scan}" Style="{StaticResource InfoLabelText}" />
                    <TextBlock Text="{Binding SelectedScannedAtText}" Style="{StaticResource InfoValueText}" />
                  </StackPanel>
                </Border>
              </WrapPanel>
              <Border Style="{StaticResource InfoCardBorderStyle}" Background="#FFF5F5F5" BorderBrush="#FFE0E0E0">
                <StackPanel>
                  <TextBlock Text="{DynamicResource Results.AcquisitionWarnings}" Style="{StaticResource InfoLabelText}" />
                  <TextBlock Text="{Binding SelectedAcquisitionWarnings}" TextWrapping="Wrap" />
                </StackPanel>
              </Border>

              <Expander Header="{DynamicResource Results.PersistentFilters}" IsExpanded="False" BorderBrush="#DDDDDD" BorderThickness="1" Padding="10"
                        ToolTip="{DynamicResource Results.PersistentFilters.ToolTip}">
                <StackPanel>
                  <TextBlock Text="{DynamicResource Results.SearchAcl}" Style="{StaticResource SubsectionHeaderText}" />
                  <Grid Margin="0,0,0,6">
                    <Grid.ColumnDefinitions>
                      <ColumnDefinition Width="Auto" />
                      <ColumnDefinition Width="*" />
                    </Grid.ColumnDefinitions>
                    <TextBlock Grid.Column="0" Text="{DynamicResource Results.AclFilter}" VerticalAlignment="Center" Margin="0,0,6,0" />
                    <TextBox Grid.Column="1" Height="26" MinWidth="220" AutomationProperties.Name="{DynamicResource Results.SearchAcl}" ToolTip="{DynamicResource Results.AclFilter.ToolTip}" Text="{Binding AclFilter, UpdateSourceTrigger=PropertyChanged}" />
                  </Grid>
                  <TextBlock Text="{DynamicResource Results.Permissions}" Style="{StaticResource SubsectionHeaderText}" />
                  <WrapPanel Margin="0,0,0,4">
                    <CheckBox Margin="0,0,12,6" Content="{DynamicResource Results.ColorizeByRight}" IsChecked="{Binding ColorizeRights}" />
                    <CheckBox Margin="0,0,12,6" Content="Allow" IsChecked="{Binding ShowAllow}" />
                    <CheckBox Margin="0,0,12,6" Content="{DynamicResource Results.Deny}" IsChecked="{Binding ShowDeny}" />
                    <CheckBox Margin="0,0,12,6" Content="{DynamicResource Results.Inherited}" IsChecked="{Binding ShowInherited}" />
                    <CheckBox Margin="0,0,12,6" Content="{DynamicResource Results.Explicit}" IsChecked="{Binding ShowExplicit}" />
                    <CheckBox Margin="0,0,12,6" Content="{DynamicResource Results.Protected}" IsChecked="{Binding ShowProtected}" />
                    <CheckBox Margin="0,0,12,6" Content="{DynamicResource Grid.Disabled}" IsChecked="{Binding ShowDisabled}" />
                  </WrapPanel>
                  <TextBlock Text="{DynamicResource Scan.Identity}" Style="{StaticResource SubsectionHeaderText}" />
                  <WrapPanel Margin="0,0,0,4">
                    <CheckBox Margin="0,0,12,6" Content="{DynamicResource Results.Everyone}" IsChecked="{Binding ShowEveryone}" />
                    <CheckBox Margin="0,0,12,6" Content="{DynamicResource Results.AuthenticatedUsers}" IsChecked="{Binding ShowAuthenticatedUsers}" />
                    <CheckBox Margin="0,0,12,6" Content="{DynamicResource Results.ServiceUsers}" IsChecked="{Binding ShowServiceAccounts}" />
                    <CheckBox Margin="0,0,12,6" Content="{DynamicResource Results.Admin}" IsChecked="{Binding ShowAdminAccounts}" />
                    <CheckBox Margin="0,0,12,6" Content="{DynamicResource Results.OtherPrincipals}" IsChecked="{Binding ShowOtherPrincipals}" />
                  </WrapPanel>
                </StackPanel>
              </Expander>
            </StackPanel>
            <TextBlock Grid.Row="1"
                       Text="{DynamicResource Results.SelectFolderForDetails}"
                       Style="{StaticResource MicrocopyText}"
                       TextWrapping="Wrap"
                       Visibility="{Binding HasNoSelectedFolder, Converter={StaticResource BooleanToVisibilityConverter}}" />
            <Grid Grid.Row="2" HorizontalAlignment="Stretch" VerticalAlignment="Stretch">
            <Border Style="{StaticResource EmptyStateBorderStyle}"
                    Visibility="{Binding HasNoScanResult, Converter={StaticResource BooleanToVisibilityConverter}}">
              <TextBlock Text="{DynamicResource Results.EmptyAfterScan}"
                         Style="{StaticResource EmptyStateText}" />
            </Border>
            <Border Style="{StaticResource EmptyStateBorderStyle}"
                    Visibility="{Binding HasNoSelectedFolder, Converter={StaticResource BooleanToVisibilityConverter}}">
              <TextBlock Text="{DynamicResource Results.SelectTreeFolder}"
                         Style="{StaticResource EmptyStateText}" />
            </Border>
            <TabControl HorizontalAlignment="Stretch" VerticalAlignment="Stretch" Visibility="{Binding HasSelectedFolder, Converter={StaticResource BooleanToVisibilityConverter}}">
              <TabItem Header="{DynamicResource Results.GroupPermissions}" ToolTip="{DynamicResource Results.FilteredGroupPermissions.ToolTip}">
                <DockPanel>
                <TextBlock DockPanel.Dock="Top"
                           Text="{DynamicResource Results.OpenGroupMembersHint}"
                           Style="{StaticResource InteractionHintText}" />
                <DataGrid ToolTip="{DynamicResource Results.FilteredGroupPermissions.ToolTip}" ItemsSource="{Binding FilteredGroupEntries}"
                          AutoGenerateColumns="False"
                          IsReadOnly="True"
                          EnableRowVirtualization="True"
                          EnableColumnVirtualization="True"
                          ScrollViewer.CanContentScroll="True"
                          ScrollViewer.HorizontalScrollBarVisibility="Auto"
                          ScrollViewer.VerticalScrollBarVisibility="Auto"
                          ColumnWidth="SizeToCells"
                          MinColumnWidth="80"
                          MouseDoubleClick="GroupEntries_OnMouseDoubleClick"
                          KeyDown="GroupEntries_OnKeyDown"
                          RowStyle="{StaticResource RightsRowStyle}"
                          HorizontalAlignment="Stretch"
                          VerticalAlignment="Stretch">
                  <DataGrid.Columns>
                    <DataGridTemplateColumn Header="Badge" CellTemplate="{StaticResource PermissionBadgesTemplate}" />
                    <DataGridTextColumn Header="Gruppo" Binding="{Binding PrincipalName}" />
                    <DataGridTextColumn Header="Owner" Binding="{Binding Owner}" />
                    <DataGridCheckBoxColumn Header="Disabilitato" Binding="{Binding IsDisabled, Mode=OneWay}" />
                    <DataGridCheckBoxColumn Header="Ereditato" Binding="{Binding IsInherited, Mode=OneWay}" />
                    <DataGridCheckBoxColumn Header="Full" Binding="{Binding HasFullControl, Mode=OneWay}" />
                    <DataGridCheckBoxColumn Header="Modify" Binding="{Binding HasModify, Mode=OneWay}" />
                    <DataGridCheckBoxColumn Header="R&amp;E" Binding="{Binding HasReadAndExecute, Mode=OneWay}" />
                    <DataGridCheckBoxColumn Header="List" Binding="{Binding HasList, Mode=OneWay}" />
                    <DataGridCheckBoxColumn Header="Read" Binding="{Binding HasRead, Mode=OneWay}" />
                    <DataGridCheckBoxColumn Header="Write" Binding="{Binding HasWrite, Mode=OneWay}" />
                    <DataGridTextColumn Header="SID" Binding="{Binding PrincipalSid}" />
                    <DataGridTextColumn Header="Risorsa" Binding="{Binding ResourceType}" />
                    <DataGridTextColumn Header="Allow/Deny" Binding="{Binding AllowDeny}" />
                    <DataGridTextColumn Header="Effective Access" Binding="{Binding EffectiveRightsSummary}" />
                    <DataGridTextColumn Header="Rischio" Binding="{Binding RiskLevel}" />
                    <DataGridTextColumn Header="SACL/Audit" Binding="{Binding AuditSummary}" />
                    <DataGridTextColumn Header="Depth" Binding="{Binding Depth}" />
                    <DataGridTextColumn Header="Inheritance" Binding="{Binding InheritanceFlags}" />
                    <DataGridTextColumn Header="Propagation" Binding="{Binding PropagationFlags}" />
                  </DataGrid.Columns>
                </DataGrid>
                </DockPanel>
              </TabItem>
              <TabItem Header="{DynamicResource Results.UserPermissions}" ToolTip="{DynamicResource Results.FilteredUserPermissions.ToolTip}">
                <DockPanel>
                <TextBlock DockPanel.Dock="Top"
                           Text="{DynamicResource Results.OpenUserGroupsHint}"
                           Style="{StaticResource InteractionHintText}" />
                <DataGrid ToolTip="{DynamicResource Results.FilteredUserPermissions.ToolTip}" ItemsSource="{Binding FilteredUserEntries}"
                          AutoGenerateColumns="False"
                          IsReadOnly="True"
                          EnableRowVirtualization="True"
                          EnableColumnVirtualization="True"
                          ScrollViewer.CanContentScroll="True"
                          ScrollViewer.HorizontalScrollBarVisibility="Auto"
                          ScrollViewer.VerticalScrollBarVisibility="Auto"
                          ColumnWidth="SizeToCells"
                          MinColumnWidth="80"
                          MouseDoubleClick="UserEntries_OnMouseDoubleClick"
                          KeyDown="UserEntries_OnKeyDown"
                          RowStyle="{StaticResource RightsRowStyle}"
                          HorizontalAlignment="Stretch"
                          VerticalAlignment="Stretch">
                  <DataGrid.Columns>
                    <DataGridTemplateColumn Header="Badge" CellTemplate="{StaticResource PermissionBadgesTemplate}" />
                    <DataGridTextColumn Header="Utente" Binding="{Binding PrincipalName}" />
                    <DataGridTextColumn Header="Owner" Binding="{Binding Owner}" />
                    <DataGridCheckBoxColumn Header="Disabilitato" Binding="{Binding IsDisabled, Mode=OneWay}" />
                    <DataGridCheckBoxColumn Header="Ereditato" Binding="{Binding IsInherited, Mode=OneWay}" />
                    <DataGridCheckBoxColumn Header="Full" Binding="{Binding HasFullControl, Mode=OneWay}" />
                    <DataGridCheckBoxColumn Header="Modify" Binding="{Binding HasModify, Mode=OneWay}" />
                    <DataGridCheckBoxColumn Header="R&amp;E" Binding="{Binding HasReadAndExecute, Mode=OneWay}" />
                    <DataGridCheckBoxColumn Header="List" Binding="{Binding HasList, Mode=OneWay}" />
                    <DataGridCheckBoxColumn Header="Read" Binding="{Binding HasRead, Mode=OneWay}" />
                    <DataGridCheckBoxColumn Header="Write" Binding="{Binding HasWrite, Mode=OneWay}" />
                    <DataGridTextColumn Header="SID" Binding="{Binding PrincipalSid}" />
                    <DataGridTextColumn Header="Risorsa" Binding="{Binding ResourceType}" />
                    <DataGridTextColumn Header="Allow/Deny" Binding="{Binding AllowDeny}" />
                    <DataGridTextColumn Header="Effective Access" Binding="{Binding EffectiveRightsSummary}" />
                    <DataGridTextColumn Header="Rischio" Binding="{Binding RiskLevel}" />
                    <DataGridTextColumn Header="SACL/Audit" Binding="{Binding AuditSummary}" />
                    <DataGridTextColumn Header="Depth" Binding="{Binding Depth}" />
                    <DataGridTextColumn Header="Inheritance" Binding="{Binding InheritanceFlags}" />
                    <DataGridTextColumn Header="Propagation" Binding="{Binding PropagationFlags}" />
                  </DataGrid.Columns>
                </DataGrid>
                </DockPanel>
              </TabItem>
              <TabItem Header="{DynamicResource Results.AllAclDetails}" ToolTip="{DynamicResource Results.AllAclDetails.ToolTip}">
                <DataGrid ToolTip="{DynamicResource Results.AllAclDetails.ToolTip}" ItemsSource="{Binding FilteredAllEntries}"
                          AutoGenerateColumns="False"
                          IsReadOnly="True"
                          EnableRowVirtualization="True"
                          EnableColumnVirtualization="True"
                          ScrollViewer.CanContentScroll="True"
                          ScrollViewer.HorizontalScrollBarVisibility="Auto"
                          ScrollViewer.VerticalScrollBarVisibility="Auto"
                          ColumnWidth="SizeToCells"
                          MinColumnWidth="80"
                          RowStyle="{StaticResource RightsRowStyle}"
                          HorizontalAlignment="Stretch"
                          VerticalAlignment="Stretch">
                  <DataGrid.Columns>
                    <DataGridTemplateColumn Header="Badge" CellTemplate="{StaticResource PermissionBadgesTemplate}" />
                    <DataGridTextColumn Header="Principal" Binding="{Binding PrincipalName}" />
                    <DataGridTextColumn Header="Owner" Binding="{Binding Owner}" />
                    <DataGridCheckBoxColumn Header="Disabilitato" Binding="{Binding IsDisabled, Mode=OneWay}" />
                    <DataGridCheckBoxColumn Header="Ereditato" Binding="{Binding IsInherited, Mode=OneWay}" />
                    <DataGridCheckBoxColumn Header="Full" Binding="{Binding HasFullControl, Mode=OneWay}" />
                    <DataGridCheckBoxColumn Header="Modify" Binding="{Binding HasModify, Mode=OneWay}" />
                    <DataGridCheckBoxColumn Header="R&amp;E" Binding="{Binding HasReadAndExecute, Mode=OneWay}" />
                    <DataGridCheckBoxColumn Header="List" Binding="{Binding HasList, Mode=OneWay}" />
                    <DataGridCheckBoxColumn Header="Read" Binding="{Binding HasRead, Mode=OneWay}" />
                    <DataGridCheckBoxColumn Header="Write" Binding="{Binding HasWrite, Mode=OneWay}" />
                    <DataGridTextColumn Header="SID" Binding="{Binding PrincipalSid}" />
                    <DataGridTextColumn Header="{DynamicResource Grid.Type}" Binding="{Binding PrincipalType}" />
                    <DataGridTextColumn Header="Risorsa" Binding="{Binding ResourceType}" />
                    <DataGridTextColumn Header="Allow/Deny" Binding="{Binding AllowDeny}" />
                    <DataGridTextColumn Header="Effective Access" Binding="{Binding EffectiveRightsSummary}" />
                    <DataGridTextColumn Header="Rischio" Binding="{Binding RiskLevel}" />
                    <DataGridTextColumn Header="SACL/Audit" Binding="{Binding AuditSummary}" />
                    <DataGridTextColumn Header="Depth" Binding="{Binding Depth}" />
                    <DataGridTextColumn Header="Inheritance" Binding="{Binding InheritanceFlags}" />
                    <DataGridTextColumn Header="Propagation" Binding="{Binding PropagationFlags}" />
                  </DataGrid.Columns>
                </DataGrid>
              </TabItem>
              <TabItem Header="{DynamicResource Results.ShareEntries}" ToolTip="{DynamicResource Results.ShareEntries.ToolTip}">
                <DataGrid ToolTip="{DynamicResource Results.ShareEntries.ToolTip}" ItemsSource="{Binding FilteredShareEntries}"
                          AutoGenerateColumns="False"
                          IsReadOnly="True"
                          EnableRowVirtualization="True"
                          EnableColumnVirtualization="True"
                          ScrollViewer.CanContentScroll="True"
                          ScrollViewer.HorizontalScrollBarVisibility="Auto"
                          ScrollViewer.VerticalScrollBarVisibility="Auto"
                          ColumnWidth="SizeToCells"
                          MinColumnWidth="80"
                          RowStyle="{StaticResource RightsRowStyle}"
                          HorizontalAlignment="Stretch"
                          VerticalAlignment="Stretch">
                  <DataGrid.Columns>
                    <DataGridTemplateColumn Header="Badge" CellTemplate="{StaticResource PermissionBadgesTemplate}" />
                    <DataGridTextColumn Header="Principal" Binding="{Binding PrincipalName}" />
                    <DataGridTextColumn Header="Owner" Binding="{Binding Owner}" />
                    <DataGridTextColumn Header="SID" Binding="{Binding PrincipalSid}" />
                    <DataGridTextColumn Header="Server" Binding="{Binding ShareServer}" />
                    <DataGridTextColumn Header="{DynamicResource Results.ShareEntries}" Binding="{Binding ShareName}" />
                    <DataGridTextColumn Header="Allow/Deny" Binding="{Binding AllowDeny}" />
                    <DataGridTextColumn Header="Depth" Binding="{Binding Depth}" />
                    <DataGridTextColumn Header="SACL/Audit" Binding="{Binding AuditSummary}" />
                  </DataGrid.Columns>
                </DataGrid>
              </TabItem>
              <TabItem Header="Effective Access" ToolTip="{DynamicResource Results.EffectiveEntries.ToolTip}">
                <DataGrid ToolTip="{DynamicResource Results.EffectiveEntries.ToolTip}" ItemsSource="{Binding FilteredEffectiveEntries}"
                          AutoGenerateColumns="False"
                          IsReadOnly="True"
                          EnableRowVirtualization="True"
                          EnableColumnVirtualization="True"
                          ScrollViewer.CanContentScroll="True"
                          ScrollViewer.HorizontalScrollBarVisibility="Auto"
                          ScrollViewer.VerticalScrollBarVisibility="Auto"
                          ColumnWidth="SizeToCells"
                          MinColumnWidth="80"
                          RowStyle="{StaticResource RightsRowStyle}"
                          HorizontalAlignment="Stretch"
                          VerticalAlignment="Stretch">
                  <DataGrid.Columns>
                    <DataGridTemplateColumn Header="Badge" CellTemplate="{StaticResource PermissionBadgesTemplate}" />
                    <DataGridTextColumn Header="Principal" Binding="{Binding PrincipalName}" />
                    <DataGridTextColumn Header="Owner" Binding="{Binding Owner}" />
                    <DataGridTextColumn Header="SID" Binding="{Binding PrincipalSid}" />
                    <DataGridTextColumn Header="{DynamicResource Results.EffectiveEntries}" Binding="{Binding EffectiveRightsSummary}" />
                    <DataGridTextColumn Header="NTFS Mask" Binding="{Binding NtfsRightsMask}" />
                    <DataGridTextColumn Header="Share Mask" Binding="{Binding ShareRightsMask}" />
                    <DataGridTextColumn Header="Depth" Binding="{Binding Depth}" />
                  </DataGrid.Columns>
                </DataGrid>
              </TabItem>
              <TabItem Header="{DynamicResource Results.PermissionInfo}" ToolTip="{DynamicResource Results.PermissionInfo.ToolTip}">
                <ScrollViewer VerticalScrollBarVisibility="Auto">
                  <StackPanel Margin="8">
                    <TextBlock Text="{DynamicResource Results.Object}" Style="{StaticResource SubsectionHeaderText}" />
                    <Border Background="#FFF5F5F5" BorderBrush="#FFE0E0E0" BorderThickness="1" CornerRadius="3" Padding="8" Margin="0,0,0,8">
                      <TextBlock Text="{Binding SelectedFolderPath}" FontWeight="SemiBold" TextWrapping="Wrap" />
                    </Border>

                    <TextBlock Text="{DynamicResource Results.AdvancedSecurity}" Style="{StaticResource SubsectionHeaderText}" />
                    <Border BorderBrush="#FFE0E0E0" BorderThickness="1" CornerRadius="3" Padding="8" Margin="0,0,0,6">
                      <Border.Style>
                        <Style TargetType="Border">
                          <Setter Property="Background" Value="#FFE8F5E9" />
                          <Style.Triggers>
                            <DataTrigger Binding="{Binding SelectedOwnerSummary}" Value="-">
                              <Setter Property="Background" Value="#FFFFEBEE" />
                            </DataTrigger>
                          </Style.Triggers>
                        </Style>
                      </Border.Style>
                      <TextBlock Text="{Binding SelectedOwnerSummary, StringFormat=Owner: {0}}" />
                    </Border>

                    <Border BorderBrush="#FFE0E0E0" BorderThickness="1" CornerRadius="3" Padding="8" Margin="0,0,0,10">
                      <Border.Style>
                        <Style TargetType="Border">
                          <Setter Property="Background" Value="#FFE8F5E9" />
                          <Style.Triggers>
                    <DataTrigger Binding="{Binding SelectedInheritanceSummary}" Value="Ereditarieta disabilitata">
                              <Setter Property="Background" Value="#FFFFEBEE" />
                            </DataTrigger>
                          </Style.Triggers>
                        </Style>
                      </Border.Style>
                      <TextBlock Text="{Binding SelectedInheritanceSummary}" />
                    </Border>

                    <TextBlock Text="{DynamicResource Results.SelectedFolder}" Style="{StaticResource SubsectionHeaderText}" />
                    <WrapPanel>
                      <Border Background="#FFE3F2FD" BorderBrush="#FFBBDEFB" BorderThickness="1" CornerRadius="3" Padding="8,6" Margin="0,0,8,8">
                        <TextBlock>
                          <Run Text="{DynamicResource Results.TotalLabel}" />
                          <Run Text="{Binding SelectedTotalAceCount, Mode=OneWay}" />
                        </TextBlock>
                      </Border>
                      <Border BorderBrush="#FFBBDEFB" BorderThickness="1" CornerRadius="3" Padding="8,6" Margin="0,0,8,8">
                        <Border.Style>
                          <Style TargetType="Border">
                            <Setter Property="Background" Value="#FFE3F2FD" />
                            <Style.Triggers>
                              <DataTrigger Binding="{Binding SelectedExplicitAceCount}" Value="0">
                                <Setter Property="Background" Value="#FFF5F5F5" />
                                <Setter Property="BorderBrush" Value="#FFE0E0E0" />
                              </DataTrigger>
                            </Style.Triggers>
                          </Style>
                        </Border.Style>
                        <TextBlock>
                          <Run Text="{DynamicResource Results.ExplicitLabel}" />
                          <Run Text="{Binding SelectedExplicitAceCount, Mode=OneWay}" />
                        </TextBlock>
                      </Border>
                      <Border BorderBrush="#FFC8E6C9" BorderThickness="1" CornerRadius="3" Padding="8,6" Margin="0,0,8,8">
                        <Border.Style>
                          <Style TargetType="Border">
                            <Setter Property="Background" Value="#FFE8F5E9" />
                            <Style.Triggers>
                              <DataTrigger Binding="{Binding SelectedInheritedAceCount}" Value="0">
                                <Setter Property="Background" Value="#FFFFF8E1" />
                                <Setter Property="BorderBrush" Value="#FFFFE082" />
                              </DataTrigger>
                            </Style.Triggers>
                          </Style>
                        </Border.Style>
                        <TextBlock>
                          <Run Text="{DynamicResource Results.InheritedLabel}" />
                          <Run Text="{Binding SelectedInheritedAceCount, Mode=OneWay}" />
                        </TextBlock>
                      </Border>
                      <Border BorderBrush="#FFFFCDD2" BorderThickness="1" CornerRadius="3" Padding="8,6" Margin="0,0,8,8">
                        <Border.Style>
                          <Style TargetType="Border">
                            <Setter Property="Background" Value="#FFFFEBEE" />
                            <Style.Triggers>
                              <DataTrigger Binding="{Binding SelectedDenyAceCount}" Value="0">
                                <Setter Property="Background" Value="#FFF5F5F5" />
                                <Setter Property="BorderBrush" Value="#FFE0E0E0" />
                              </DataTrigger>
                            </Style.Triggers>
                          </Style>
                        </Border.Style>
                        <TextBlock>
                          <Run Text="{DynamicResource Results.DenyLabel}" />
                          <Run Text="{Binding SelectedDenyAceCount, Mode=OneWay}" />
                        </TextBlock>
                      </Border>
                    </WrapPanel>
                  </StackPanel>
                </ScrollViewer>
              </TabItem>
              <TabItem Header="{DynamicResource Results.ErrorsTab}" ToolTip="{DynamicResource Results.ErrorsTab.ToolTip}">
                <Grid HorizontalAlignment="Stretch" VerticalAlignment="Stretch">
                  <DataGrid ToolTip="{DynamicResource Results.ErrorsGrid.ToolTip}" ItemsSource="{Binding FilteredErrors}"
                            AutoGenerateColumns="False"
                            IsReadOnly="True"
                            EnableRowVirtualization="True"
                            EnableColumnVirtualization="True"
                            ScrollViewer.CanContentScroll="True"
                            ScrollViewer.HorizontalScrollBarVisibility="Auto"
                            ScrollViewer.VerticalScrollBarVisibility="Auto"
                            ColumnWidth="SizeToCells"
                            MinColumnWidth="80"
                            HorizontalAlignment="Stretch"
                            VerticalAlignment="Stretch">
                    <DataGrid.Columns>
                      <DataGridTextColumn Header="Path" Binding="{Binding Path}" />
                      <DataGridTextColumn Header="{DynamicResource Grid.Type}" Binding="{Binding ErrorType}" />
                      <DataGridTextColumn Header="{DynamicResource Grid.Message}" Binding="{Binding Message}" />
                    </DataGrid.Columns>
                  </DataGrid>
                </Grid>
              </TabItem>
            </TabControl>
            </Grid>
          </Grid>
        </Border>
</UserControl>
