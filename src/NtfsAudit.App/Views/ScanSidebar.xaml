<UserControl x:Class="NtfsAudit.App.Views.ScanSidebar"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">
  <Border BorderBrush="#DDDDDD" BorderThickness="1" Padding="12" HorizontalAlignment="Stretch" VerticalAlignment="Stretch">
          <Grid>
            <Grid.RowDefinitions>
              <RowDefinition Height="Auto" />
              <RowDefinition Height="*" />
            </Grid.RowDefinitions>

            <GroupBox Grid.Row="0" Header="Stato scansione" BorderBrush="#DDDDDD" BorderThickness="1" Padding="10">
              <StackPanel>
                <StackPanel Orientation="Horizontal" Margin="0,0,0,8">
                  <Border Width="14" Height="14" Background="{Binding StatusBrush}" CornerRadius="2" Margin="0,0,6,0" />
                  <TextBlock Text="{Binding StatusText}" FontWeight="SemiBold" VerticalAlignment="Center" />
                </StackPanel>
                <Border Background="{Binding CurrentPathBackground}" Padding="6" CornerRadius="4" Margin="0,0,0,8">
                  <TextBlock Text="{Binding CurrentPathText}" TextTrimming="CharacterEllipsis" />
                </Border>
                <WrapPanel Margin="0,0,0,6">
                  <Border Padding="6,4" CornerRadius="4" Margin="0,0,8,6" Background="#FFE8F5E9">
                    <StackPanel Orientation="Horizontal">
                      <TextBlock Text="Cartelle analizzate: " VerticalAlignment="Center" />
                      <TextBlock Text="{Binding ProcessedCount}" VerticalAlignment="Center" />
                    </StackPanel>
                  </Border>
                  <Border Padding="6,4" CornerRadius="4" Margin="0,0,8,6" Background="#FFE3F2FD">
                    <StackPanel Orientation="Horizontal">
                      <TextBlock Text="File analizzati: " VerticalAlignment="Center" />
                      <TextBlock Text="{Binding ProcessedFilesCount}" VerticalAlignment="Center" />
                    </StackPanel>
                  </Border>
                  <Border Padding="6,4" CornerRadius="4" Margin="0,0,8,6" Background="#FFFFEBEE">
                    <StackPanel Orientation="Horizontal">
                      <TextBlock Text="Errori: " VerticalAlignment="Center" />
                      <TextBlock Text="{Binding ErrorCount}" VerticalAlignment="Center" />
                    </StackPanel>
                  </Border>
                  <Border Padding="6,4" CornerRadius="4" Margin="0,0,8,6" Background="#FFF5F5F5">
                    <StackPanel Orientation="Horizontal">
                      <TextBlock Text="Tempo: " VerticalAlignment="Center" />
                      <TextBlock Text="{Binding ElapsedText}" VerticalAlignment="Center" />
                    </StackPanel>
                  </Border>
                </WrapPanel>
              </StackPanel>
            </GroupBox>

            <ScrollViewer Grid.Row="1" VerticalScrollBarVisibility="Auto" Margin="0,10,0,0">
              <StackPanel>
              <GroupBox Header="Scansione" BorderBrush="#DDDDDD" BorderThickness="1" Padding="10" Margin="0,0,0,0" IsEnabled="{Binding IsScanConfigEnabled}" Visibility="{Binding IsNotViewerMode, Converter={StaticResource BooleanToVisibilityConverter}}">
                <StackPanel>
                  <TextBlock Text="Cartelle da scansionare" Style="{StaticResource SubsectionHeaderText}" />
                  <TextBlock Text="Puoi fermare la scansione/job (con pulizia residui), modificare questo elenco e riavviare con nuove cartelle." Style="{StaticResource MicrocopyText}" />
                  <Grid Margin="0,0,0,8">
                    <Grid.ColumnDefinitions>
                      <ColumnDefinition Width="Auto" />
                      <ColumnDefinition Width="*" />
                      <ColumnDefinition Width="Auto" />
                    </Grid.ColumnDefinitions>
                    <Button Grid.Column="0" Margin="0,0,8,0" Padding="10,4" Content="Sfoglia..." Command="{Binding BrowseCommand}" />
                    <TextBox Grid.Column="1" Height="26" VerticalAlignment="Center" MinWidth="160" Text="{Binding RootPath, UpdateSourceTrigger=PropertyChanged}" />
                    <Button Grid.Column="2" Margin="8,0,0,0" Padding="10,4" Content="Aggiungi" Command="{Binding AddScanRootCommand}" />
                  </Grid>
                  <Grid Margin="0,0,0,8">
                    <Grid.ColumnDefinitions>
                      <ColumnDefinition Width="*" />
                      <ColumnDefinition Width="Auto" />
                    </Grid.ColumnDefinitions>
                    <ListBox Grid.Column="0" Height="90" ItemsSource="{Binding ScanRoots}" SelectedItem="{Binding SelectedScanRoot}">
                      <ListBox.ItemTemplate>
                        <DataTemplate>
                          <Border Padding="4" Margin="0,1" CornerRadius="3" Background="{Binding Converter={StaticResource ScanRootPathKindBrushConverter}}">
                            <DockPanel>
                              <StackPanel Orientation="Horizontal" DockPanel.Dock="Right" VerticalAlignment="Center">
                                <Border Background="#FF37474F" CornerRadius="8" Padding="6,1" Margin="4,0,0,0">
                                  <TextBlock Text="{Binding Converter={StaticResource ScanRootPathKindLabelConverter}}" Foreground="White" FontSize="10" />
                                </Border>
                                <Border Background="#FF6A1B9A" CornerRadius="8" Padding="6,1" Margin="4,0,0,0"
                                        Visibility="{Binding Converter={StaticResource DfsMultiServerVisibilityConverter}}">
                                  <TextBlock Text="DFS multi-server" Foreground="White" FontSize="10" />
                                </Border>
                              </StackPanel>
                              <TextBlock Text="{Binding}" VerticalAlignment="Center" Margin="0,0,6,0" TextTrimming="CharacterEllipsis"/>
                            </DockPanel>
                          </Border>
                        </DataTemplate>
                      </ListBox.ItemTemplate>
                    </ListBox>
                    <Button Grid.Column="1" Margin="8,0,0,0" Padding="10,4" Content="Rimuovi" VerticalAlignment="Top" Command="{Binding RemoveScanRootCommand}" />
                  </Grid>
                  <WrapPanel Margin="0,0,0,8">
                    <Button Margin="0,0,8,0" Padding="10,4" Content="Salva set cartelle" Command="{Binding SaveScanRootSetCommand}" />
                    <Button Padding="10,4" Content="Carica set cartelle" Command="{Binding LoadScanRootSetCommand}" />
                  </WrapPanel>
                  <Grid Margin="0,0,0,6" Visibility="{Binding HasSelectedScanRootDfsTargets, Converter={StaticResource BooleanToVisibilityConverter}}">
                    <Grid.ColumnDefinitions>
                      <ColumnDefinition Width="Auto" />
                      <ColumnDefinition Width="*" />
                    </Grid.ColumnDefinitions>
                    <TextBlock Grid.Column="0" Text="Target DFS cartella selezionata" VerticalAlignment="Center" Margin="0,0,6,0" />
                    <ComboBox Grid.Column="1"
                              Height="24"
                              Background="#FFE3F2FD"
                              ItemsSource="{Binding SelectedScanRootDfsTargets}"
                              SelectedItem="{Binding SelectedScanRootDfsTarget, UpdateSourceTrigger=PropertyChanged}" />
                  </Grid>
                  <TextBlock Text="Output report .ntaudit" Style="{StaticResource SubsectionHeaderText}" />
                  <Grid Margin="0,0,0,8">
                    <Grid.ColumnDefinitions>
                      <ColumnDefinition Width="Auto" />
                      <ColumnDefinition Width="*" />
                    </Grid.ColumnDefinitions>
                    <Button Grid.Column="0" Margin="0,0,8,0" Padding="10,4" Content="Sfoglia..." Command="{Binding BrowseOutputDirectoryCommand}" />
                    <TextBox Grid.Column="1" Height="26" VerticalAlignment="Center" MinWidth="160" Text="{Binding AuditOutputDirectory, UpdateSourceTrigger=PropertyChanged}" />
                  </Grid>
                  <CheckBox Margin="0,0,0,4" Content="Esegui tramite servizio Windows" IsChecked="{Binding UseWindowsServiceMode}" ToolTip="Usa l'host Windows opzionale per eseguire la scansione in background; non aggiunge funzioni di analisi rispetto alla modalita locale." />
                  <TextBlock Text="Opzionale: utile per scansioni lunghe o non interattive; usa la stessa logica dell'app e non aggiunge funzionalita applicative." Style="{StaticResource MicrocopyText}" Margin="0,0,0,6" TextWrapping="Wrap" />
                  <TextBlock Text="Credenziali scansione" Style="{StaticResource SubsectionHeaderText}" />
                  <TextBlock Text="{Binding GlobalCredentialStatusText}" Style="{StaticResource MicrocopyText}" Margin="0,0,0,6" />
                  <Grid Margin="0,0,0,6">
                    <Grid.ColumnDefinitions>
                      <ColumnDefinition Width="Auto" />
                      <ColumnDefinition Width="*" />
                      <ColumnDefinition Width="Auto" />
                      <ColumnDefinition Width="Auto" />
                    </Grid.ColumnDefinitions>
                    <TextBlock Grid.Column="0" Text="Globali" VerticalAlignment="Center" Margin="0,0,6,0" />
                    <TextBox Grid.Column="1" Height="26" Margin="0,0,8,0" MinWidth="180" Text="{Binding GlobalCredentialUserName, UpdateSourceTrigger=PropertyChanged}" ToolTip="Utente globale (DOMINIO\utente o utente@dominio)" />
                    <PasswordBox x:Name="GlobalCredentialPasswordBox" Grid.Column="2" Height="26" Width="180" Margin="0,0,8,0" PasswordChanged="GlobalCredentialPasswordBox_OnPasswordChanged" ToolTip="Password credenziali globali" />
                    <StackPanel Grid.Column="3" Orientation="Horizontal">
                      <Button Margin="0,0,8,0" Padding="10,4" Content="Salva" Command="{Binding SaveGlobalCredentialCommand}" />
                      <Button Padding="10,4" Content="Rimuovi" Command="{Binding ClearGlobalCredentialCommand}" />
                    </StackPanel>
                  </Grid>
                  <TextBlock Text="{Binding SelectedScanRootCredentialStatusText}" Style="{StaticResource MicrocopyText}" Margin="0,0,0,4" />
                  <TextBlock Text="{Binding SelectedScanRootEffectiveCredentialSource}" Style="{StaticResource MicrocopyText}" Margin="0,0,0,6" />
                  <Grid Margin="0,0,0,8" IsEnabled="{Binding HasSelectedScanRoot}">
                    <Grid.ColumnDefinitions>
                      <ColumnDefinition Width="Auto" />
                      <ColumnDefinition Width="*" />
                      <ColumnDefinition Width="Auto" />
                      <ColumnDefinition Width="Auto" />
                    </Grid.ColumnDefinitions>
                    <TextBlock Grid.Column="0" Text="Override root" VerticalAlignment="Center" Margin="0,0,6,0" />
                    <TextBox Grid.Column="1" Height="26" Margin="0,0,8,0" MinWidth="180" Text="{Binding SelectedScanRootCredentialUserName, UpdateSourceTrigger=PropertyChanged}" ToolTip="Utente dedicato per la root selezionata" />
                    <PasswordBox x:Name="SelectedScanRootCredentialPasswordBox" Grid.Column="2" Height="26" Width="180" Margin="0,0,8,0" PasswordChanged="SelectedScanRootCredentialPasswordBox_OnPasswordChanged" ToolTip="Password override root" />
                    <StackPanel Grid.Column="3" Orientation="Horizontal">
                      <Button Margin="0,0,8,0" Padding="10,4" Content="Salva" Command="{Binding SaveScanRootCredentialCommand}" />
                      <Button Padding="10,4" Content="Rimuovi" Command="{Binding ClearScanRootCredentialCommand}" />
                    </StackPanel>
                  </Grid>
                  <Expander Header="Opzioni principali" IsExpanded="True">
                    <StackPanel Margin="0,6,0,0">
                      <Grid Margin="0,0,0,6">
                        <Grid.ColumnDefinitions>
                          <ColumnDefinition Width="Auto" />
                          <ColumnDefinition Width="Auto" />
                          <ColumnDefinition Width="*" />
                        </Grid.ColumnDefinitions>
                        <TextBlock Grid.Column="0" Text="Profondita" VerticalAlignment="Center" Margin="0,0,6,0" />
                        <TextBox Grid.Column="1" Width="60" Height="24" Text="{Binding MaxDepth}" IsEnabled="{Binding IsMaxDepthEnabled}" Margin="0,0,12,0" />
                        <CheckBox Grid.Column="2" Content="Analizza tutte le sottocartelle" IsChecked="{Binding ScanAllDepths}" />
                      </Grid>
                      <CheckBox Content="Includi permessi ereditati" IsChecked="{Binding IncludeInherited}" />
                    </StackPanel>
                  </Expander>
                </StackPanel>
              </GroupBox>

              <GroupBox Header="Opzioni" BorderBrush="#DDDDDD" BorderThickness="1" Padding="10" Margin="0,10,0,0" IsEnabled="{Binding IsScanConfigEnabled}" Visibility="{Binding IsNotViewerMode, Converter={StaticResource BooleanToVisibilityConverter}}">
                <StackPanel>
                  <TextBlock Text="Identita" Style="{StaticResource SubsectionHeaderText}" />
                  <CheckBox Margin="0,0,0,4" Content="Risolvi identita" IsChecked="{Binding ResolveIdentities}" />
                  <TextBlock Text="Aumenta accuratezza, puo impattare performance." Style="{StaticResource MicrocopyText}" />
                  <CheckBox Margin="0,0,0,4" Content="Espandi gruppi annidati" IsChecked="{Binding ExpandGroups}" IsEnabled="{Binding IsExpandGroupsEnabled}" />
                  <CheckBox Margin="0,0,0,4" Content="Usa PowerShell per AD" IsChecked="{Binding UsePowerShell}" IsEnabled="{Binding IsIdentityOptionsEnabled}" />
                  <Expander Header="Opzioni avanzate (per amministratori)" IsExpanded="True" Margin="0,8,0,0">
                    <StackPanel Margin="0,6,0,0">
                      <CheckBox Margin="0,0,0,4" Content="Abilita audit avanzato" IsChecked="{Binding EnableAdvancedAudit}" />
                      <StackPanel Margin="0,4,0,0" IsEnabled="{Binding IsAdvancedAuditEnabled}">
                      <CheckBox Margin="0,0,0,4" Content="Calcola Effective Access" IsChecked="{Binding ComputeEffectiveAccess}" />
                      <CheckBox Margin="0,0,0,4" Content="Leggi permessi Share (SMB)" IsChecked="{Binding IncludeSharePermissions}" />
                      <CheckBox Margin="0,0,0,4" Content="Scansiona file" IsChecked="{Binding IncludeFiles}" />
                      <CheckBox Margin="0,0,0,4" Content="Leggi Owner e SACL" IsChecked="{Binding ReadOwnerAndSacl}" />
                      <CheckBox Content="Confronta con baseline/policy attese" IsChecked="{Binding CompareBaseline}" />
                      </StackPanel>
                    </StackPanel>
                  </Expander>
                  <TextBlock Text="Esclusioni" Style="{StaticResource SubsectionHeaderText}" />
                  <CheckBox Margin="0,0,0,4" Content="Escludi utenti di servizio" IsChecked="{Binding ExcludeServiceAccounts}" IsEnabled="{Binding IsIdentityOptionsEnabled}" />
                  <CheckBox Content="Escludi utenti admin" IsChecked="{Binding ExcludeAdminAccounts}" IsEnabled="{Binding IsIdentityOptionsEnabled}" />
                </StackPanel>
              </GroupBox>
              </StackPanel>
            </ScrollViewer>
          </Grid>
        </Border>
</UserControl>
